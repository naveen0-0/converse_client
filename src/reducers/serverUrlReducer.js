const initialState = {
  serverUrl : "http://localhost:5000",
}

export const serverUrlReducer = (state = initialState,action) => {
  switch (action.type) {
      default:
          return state;
  }
}