const initialState = {
  serverUrl : "https://converse-1910.herokuapp.com",
}

export const serverUrlReducer = (state = initialState,action) => {
  switch (action.type) {
      default:
          return state;
  }
}